'use client'

import { Button } from '../ui/button'

export default function ConnectWalletButton() {
  return (
    <Button onClick={() => {}} className="connect-wallet-button">
      Connect Wallet
    </Button>
  )
}
